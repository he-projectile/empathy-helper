seed: 42
trainer:
  max_epochs: 10
  accelerator: gpu
  devices: 1
model:
  embedding_dim: 128
  hidden_dim: 128
  num_layers: 2
  dropout: 0.3
  learning_rate: 0.001
data:
  batch_size: 64
  max_length: 64
  data_dir: data/raw
  num_workers: 4
